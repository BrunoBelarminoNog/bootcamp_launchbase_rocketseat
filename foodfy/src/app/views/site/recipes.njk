{% extends "layout.njk" %}

{% block styles %}
    <link rel="stylesheet" href="/styles/cards.css">
{% endblock styles %}

{% block content %}

    <section id="receitas" class="page_receitas">

        {% if filter %}
            <div class="receitas_cabecalho">
                <h2>Buscando por "{{filter}}"</h2>
            </div>
        {% endif %}

        <div class="receitas_cards">

            {% for item in items %}
                <div class="card" id="{{item.name}}" onclick="abrirReceita({{item.id}})">
                    <a>
                        <div class="card_img">
                            <img src="{{item.image}}" alt="">
                        </div>
                        <div class="card_content">
                            <h3>{{item.title}}</h3>
                            <p>{{item.chef_id}}</p>
                        </div>
                    </a>
                </div>
            {% endfor %}

        </div>

    </section>

{% endblock content %}